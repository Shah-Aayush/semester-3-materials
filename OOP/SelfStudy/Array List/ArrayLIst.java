import java.util.Arraylist;
class ArrayList {
	public static void main(String[] args) {
			int simple_array[] = new int[5]; 	// like this : {1,2,3,4,5)
			ArrayList<Integer> myList = new ArrayList<Integer>(5);
			
	}
}
/*
->The problem in the simple array is we have to define the size of array at the initialization. the size of the array at this case if fixed and its hard in real programming to change this size of the array using simple array
->Java contains library named util which contains the keyword arraylist, this array list will help us to maintain and use dynamic arrays so unlike
	the simple arrays
*/