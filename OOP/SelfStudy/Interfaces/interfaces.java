class Interfaces {
	public static void main(String[] args) {
		Bank abc = new Bank_ABC();
		System.out.println(abc.getInterestRate());
	}
}
/*
INTERFACES : 
->Interface is same as abstract class [It also cannot be instancieted]

->If we have two CLASSES then we have to use EXTENDS 
->If we have INTERFACE and CLASS then we have to use IMPLEMENTS 
->If we have INTERFACE and INTERFACE than we have to use EXTENDS
*/